[
    {
        "key": "ctrl+alt+c",
        "command": "workbench.action.tasks.runTask",
        "args": "▶ Chrome",
        "when": "editorFocus || terminalFocus || !editorFocus"
    },
    {
        "key": "ctrl+alt+d",
        "command": "workbench.action.tasks.runTask",
        "args": "▶ Dev",
        "when": "editorFocus || terminalFocus || !editorFocus"
    },
    {
        "key": "ctrl+alt+l",
        "command": "workbench.action.tasks.runTask",
        "args": "▶ Claude",
        "when": "editorFocus || terminalFocus || !editorFocus"
    }
]