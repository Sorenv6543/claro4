---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified: [src/utils/icalParser.ts, src/composables/owner/usePropertySync.ts, src/stores/booking.ts, src/pages/owner/Bookings.vue]
autonomous: true
requirements: [SYNC-01, SYNC-02]

must_haves:
  truths:
    - "Bookings are automatically ingested from an iCal URL"
    - "Overlapping bookings are identified as conflicts"
    - "Bookings are persistent and tenant-isolated"
  artifacts:
    - path: "src/utils/icalParser.ts"
      provides: "iCal to Booking object transformation"
    - path: "src/stores/booking.ts"
      provides: "Booking state and sync actions"
  key_links:
    - from: "src/composables/owner/usePropertySync.ts"
      to: "src/utils/icalParser.ts"
      via: "parseICal(data)"
---

<objective>
Implement the iCal sync engine to automate booking ingestion. This includes the parser, sync logic, conflict detection, and a calendar view for owners.

Purpose: Automate the populating of the cleaning schedule.
Output: iCal parser, booking store, and calendar view.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: iCal Parser Utility</name>
  <files>src/utils/icalParser.ts</files>
  <action>
    Implement a utility to parse iCal data strings into application-specific Booking objects.
    - Handle check-in and check-out dates.
    - Extract guest names or summary info where possible.
    - Support common OTA formats (Airbnb, VRBO, Booking.com).
    - Use a lightweight library or regex-based parser suitable for the browser/edge environment.
  </action>
  <verify>
    <automated>npm test src/utils/icalParser.test.ts</automated>
  </verify>
  <done>iCal data is correctly parsed into booking objects.</done>
</task>

<task type="auto">
  <name>Task 2: Booking Store & Sync Logic</name>
  <files>src/stores/booking.ts, src/composables/owner/usePropertySync.ts</files>
  <action>
    Implement `useBookingStore` and `usePropertySync`.
    - `useBookingStore`: CRUD for bookings, fetch bookings for a tenant.
    - `usePropertySync`: Fetch iCal data from the URL, parse it, and upsert records into the `bookings` table.
    - Implement conflict detection: flag any new booking that overlaps with an existing confirmed booking in the same property.
  </action>
  <verify>
    <automated>npm test src/stores/booking.test.ts</automated>
  </verify>
  <done>Bookings are successfully ingested and conflicts are detected.</done>
</task>

<task type="auto">
  <name>Task 3: Owner Booking Calendar View</name>
  <files>src/pages/owner/Bookings.vue</files>
  <action>
    Implement the booking calendar for owners using `@fullcalendar/vue3`.
    - Show bookings in a monthly or list view.
    - Visually differentiate conflicting bookings (e.g., amber/coral color).
    - Allow clicking a booking to view details.
  </action>
  <verify>
    <automated>npm test src/pages/owner/Bookings.test.ts</automated>
  </verify>
  <done>Owners can see their property bookings and conflicts on a visual calendar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External iCal URL → Application | iCal data is untrusted. The parser must handle malformed input gracefully and avoid injection risks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Denial of Service | iCal Parser | mitigate | Limit the size of iCal data processed and implement timeouts for external fetches. |
| T-01-07 | Tampering | Booking Ingestion | mitigate | Ensure the sync logic strictly enforces the `tenant_id` for every upserted booking. |
</threat_model>

<verification>
Manual verification: Enter a valid Airbnb iCal URL for a property and verify that bookings appear on the calendar.
</verification>

<success_criteria>
- Bookings are successfully ingested from external iCal feeds.
- Conflict detection flags overlapping dates.
- Calendar view provides a clear overview of property availability.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done
</output>
