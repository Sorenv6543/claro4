---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/stores/auth.ts, src/router/index.ts, src/router/guards.ts, src/layouts/owner.vue, src/layouts/admin.vue]
autonomous: true
requirements: [AUTH-01, AUTH-02]

must_haves:
  truths:
    - "Users are redirected to their role-specific dashboard after login"
    - "Unauthorized users cannot access protected routes"
    - "Tenant ID is available in the auth store"
  artifacts:
    - path: "src/stores/auth.ts"
      provides: "Reactive user, role, and tenant state"
    - path: "src/router/guards.ts"
      provides: "Navigation guards for RBAC"
  key_links:
    - from: "src/router/index.ts"
      to: "src/router/guards.ts"
      via: "router.beforeEach"
---

<objective>
Implement secure authentication handling and role-based access control (RBAC) in the frontend. Ensure users are isolated into their respective tenant contexts upon login.

Purpose: Secure entry and navigation isolation.
Output: Auth store, router guards, and role-based layouts.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Role-Aware Auth Store</name>
  <files>src/stores/auth.ts</files>
  <action>
    Implement `useAuthStore` using Pinia.
    It should:
    - Synchronize with Supabase Auth state.
    - Fetch and store the user's profile (including `role` and `tenant_id`) from the `profiles` table.
    - Provide computed properties like `isAdmin`, `isOwner`, `isCleaner`.
    - Handle session persistence.
  </action>
  <verify>
    <automated>npm test src/stores/auth.test.ts</automated>
  </verify>
  <done>Auth store manages user state, role, and tenant ID correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Navigation Guards & Role-Based Routing</name>
  <files>src/router/guards.ts, src/router/index.ts</files>
  <action>
    Create `src/router/guards.ts` with `setupRouterGuards(router)`.
    Implement logic to:
    - Redirect unauthenticated users to `/auth/login`.
    - Check route meta `requiresRole` and redirect if the user doesn't have the required role.
    - Redirect users to their default dashboard if they try to access a role-inappropriate path (e.g., owner trying to access `/admin`).
    Update `src/router/index.ts` to include meta tags for roles.
  </action>
  <verify>
    <automated>npm test src/router/guards.test.ts</automated>
  </verify>
  <done>Router prevents unauthorized access and enforces RBAC.</done>
</task>

<task type="auto">
  <name>Task 3: Role-Specific Layouts</name>
  <files>src/layouts/owner.vue, src/layouts/admin.vue, src/layouts/auth.vue</files>
  <action>
    Implement base layouts for Owner and Admin views.
    - `owner.vue`: Mobile-optimized sidebar/bottom-nav for property owners.
    - `admin.vue`: Desktop-optimized sidebar for dispatchers.
    - `auth.vue`: Clean layout for login/signup pages.
    Use Vuetify components as per `DESIGN.md` (rounded corners, Studio Violet accent).
  </action>
  <verify>
    <automated>npm test src/layouts/Layouts.test.ts</automated>
  </verify>
  <done>Layouts provide role-specific UI shells.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Router → Protected Pages | Client-side routing is a UI convenience; data is secured by RLS, but UI must still prevent unauthorized access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Elevation of Privilege | Router Guards | mitigate | Ensure router guards validate roles against the auth store. |
| T-01-04 | Information Disclosure | Layouts | mitigate | Do not render admin-specific UI elements in owner layouts. |
</threat_model>

<verification>
Manual login test as Admin, Owner, and Cleaner to verify redirection and layout application.
</verification>

<success_criteria>
- Users can log in and out.
- Profile data (role/tenant) is correctly loaded into state.
- Unauthorized route access is blocked.
- Correct layout is rendered based on user role.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done
</output>
