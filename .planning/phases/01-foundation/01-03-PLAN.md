---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified: [src/stores/property.ts, src/pages/owner/Properties.vue, src/pages/owner/PropertyDetails.vue, src/components/smart/owner/PropertyForm.vue]
autonomous: true
requirements: [PROP-01]

must_haves:
  truths:
    - "Owners can view a list of their properties"
    - "Owners can add, edit, and delete properties"
    - "Property data is persistent and tenant-isolated"
  artifacts:
    - path: "src/stores/property.ts"
      provides: "CRUD actions and reactive property state"
    - path: "src/pages/owner/Properties.vue"
      provides: "Property management UI"
  key_links:
    - from: "src/pages/owner/Properties.vue"
      to: "src/stores/property.ts"
      via: "storeToRefs and actions"
---

<objective>
Implement the property management system for owners. This includes the data store, property list view, and CRUD forms.

Purpose: Enable owners to define the assets they manage.
Output: Property store and management UI pages.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Property Store</name>
  <files>src/stores/property.ts</files>
  <action>
    Implement `usePropertyStore` using Pinia.
    - Use `Map` for state to ensure O(1) access.
    - Implement `fetchProperties`, `createProperty`, `updateProperty`, and `deleteProperty` actions.
    - Ensure all operations include `tenant_id` from the auth store.
    - Implement basic error handling and optimistic updates.
  </action>
  <verify>
    <automated>npm test src/stores/property.test.ts</automated>
  </verify>
  <done>Property store handles CRUD operations and maintains tenant isolation.</done>
</task>

<task type="auto">
  <name>Task 2: Property List & Details UI</name>
  <files>src/pages/owner/Properties.vue, src/pages/owner/PropertyDetails.vue</files>
  <action>
    Create the property list page (`Properties.vue`) displaying a grid of cards (Vuetify `v-card`).
    Create the property details page (`PropertyDetails.vue`) for viewing a single property.
    Use `src/layouts/owner.vue`.
  </action>
  <verify>
    <automated>npm test src/pages/owner/Properties.test.ts</automated>
  </verify>
  <done>Property list and details pages are functional and styled.</done>
</task>

<task type="auto">
  <name>Task 3: Property CRUD Form</name>
  <files>src/components/smart/owner/PropertyForm.vue</files>
  <action>
    Create a reusable form component for adding and editing properties.
    Fields: Name, Address, iCal URL (optional), Metadata.
    Include validation (e.g., name is required).
    Use Vuetify form components.
  </action>
  <verify>
    <automated>npm test src/components/smart/owner/PropertyForm.test.ts</automated>
  </verify>
  <done>Property form allows creating and editing property data with validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI Form → Store | Input must be validated in the store/utility layer before being sent to Supabase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Information Disclosure | Property Store | mitigate | Ensure every store request filters by the current user's tenant_id (defense in depth alongside RLS). |
</threat_model>

<verification>
Manual verification: Add a property, edit it, and verify it appears in the list and reflects changes.
</verification>

<success_criteria>
- Owners can successfully perform CRUD on properties.
- Data is correctly saved to Supabase with the appropriate tenant ID.
- UI follows the Material Design rules (rounded-sm containers, pill interactive elements).
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done
</output>
