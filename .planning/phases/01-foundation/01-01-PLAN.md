---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20240101000000_core_schema.sql
  - supabase/tests/database/tenant_isolation.test.sql
autonomous: true
requirements: [AUTH-01, AUTH-02, MT-01, MT-02, PROP-01, SYNC-01, SYNC-02, DB-01]

must_haves:
  truths:
    - "Tenant isolation is enforced via RLS using session-cached tenant IDs"
    - "Profiles are immutable regarding role and tenant assignment after creation"
    - "tenant_id auto-populates on insertion for properties and bookings"
    - "Cross-tenant data access is blocked at the SQL level"
  artifacts:
    - path: "supabase/migrations/20240101000000_core_schema.sql"
      provides: "Tables, Indexes, RLS, and Triggers"
    - path: "supabase/tests/database/tenant_isolation.test.sql"
      provides: "pgTAP test suite for security validation"
  key_links:
    - from: "properties.tenant_id"
      to: "tenants.id"
      via: "Foreign Key + Index"
    - from: "bookings.tenant_id"
      to: "tenants.id"
      via: "Foreign Key + Index"
---

<objective>
Establish the secure multi-tenant foundation for Claro. This involves creating the core relational schema, implementing high-performance RLS with session caching, and enforcing data integrity through immutable security triggers.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core Schema, Indexes, and RLS Optimization</name>
  <files>supabase/migrations/20240101000000_core_schema.sql</files>
  <action>
    - Create `tenants`, `profiles`, `properties`, and `bookings` tables per Requirements DB-01/MT-01.
    - Implement `get_my_tenant_id()` function with `SECURITY DEFINER` and session caching using `current_setting` and `set_config('app.current_tenant_id', ...)` to minimize profile lookups.
    - Add B-tree indexes on `tenant_id` for all multi-tenant tables.
    - Enable RLS on all tables and implement policies that filter by `tenant_id = get_my_tenant_id()`.
  </action>
  <verify>
    <automated>cat supabase/migrations/20240101000000_core_schema.sql | grep -i "get_my_tenant_id"</automated>
  </verify>
  <done>Schema exists with optimized RLS and required indexes.</done>
</task>

<task type="auto">
  <name>Task 2: Security Hardening and Automation Triggers</name>
  <files>supabase/migrations/20240101000000_core_schema.sql</files>
  <action>
    - Implement a `BEFORE UPDATE` trigger on `profiles` to block changes to `role` or `tenant_id` (enforcing immutability per security requirements).
    - Implement `BEFORE INSERT` triggers on `properties` and `bookings` that execute `NEW.tenant_id := get_my_tenant_id()` to ensure auto-population.
    - Ensure all triggers use `plpgsql` and handle edge cases where `get_my_tenant_id()` might be null for unauthenticated inserts (block them).
  </action>
  <verify>
    <automated>cat supabase/migrations/20240101000000_core_schema.sql | grep -E "TRIGGER.*BEFORE UPDATE.*profiles"</automated>
  </verify>
  <done>Security triggers protect role/tenant fields and automate data entry.</done>
</task>

<task type="auto">
  <name>Task 3: pgTAP Security and Isolation Verification</name>
  <files>supabase/tests/database/tenant_isolation.test.sql</files>
  <action>
    - Create a pgTAP test suite to validate:
      1. Isolation: Users in Tenant A cannot see rows from Tenant B.
      2. Auto-population: Inserting a booking without a `tenant_id` correctly assigns the user's `tenant_id`.
      3. Immutability: Attempting to update a profile's `role` via SQL results in an error or no change (depending on trigger logic).
    - Use `tests.authenticate_as('user_uuid')` or manual JWT claim simulation in the test file.
  </action>
  <verify>
    <automated>supabase test db --file supabase/tests/database/tenant_isolation.test.sql</automated>
  </verify>
  <done>pgTAP tests pass, confirming isolation and trigger enforcement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → Database | Untrusted tenant context must be strictly isolated via RLS |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | RLS Policies | mitigate | Use session-cached `get_my_tenant_id()` for mandatory filtering |
| T-01-02 | Tampering | Profiles Table | mitigate | `BEFORE UPDATE` trigger blocks modification of sensitive fields |
| T-01-03 | Elevation of Privilege | Profiles.role | mitigate | Immutability trigger prevents role escalation after initial assignment |
</threat_model>

<verification>
- Run `supabase db lint` to check for syntax errors.
- Run `supabase test db` to execute pgTAP suites.
- Verify `tenant_id` indexes exist: `SELECT * FROM pg_indexes WHERE tablename = 'bookings';`
</verification>

<success_criteria>
- All tables (`tenants`, `profiles`, `properties`, `bookings`) are created and RLS-enabled.
- `get_my_tenant_id()` function correctly caches and returns the UUID.
- pgTAP tests confirm that cross-tenant access is impossible.
- Immutability triggers prevent users from changing their own roles.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
