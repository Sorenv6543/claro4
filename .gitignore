node_modules/
.console-ninja*

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/launch.json
!.vscode/tasks.json
!.vscode/mcp.json
!.vscode/keybindings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files
.env
.env.*
!.env.example

# Build output
/dist
/build

# Coverage directory
/coverage

# Cache directories
.cache
.temp
.tmp

# Playwright
/test-results/
/playwright-report/
/playwright/.cache/

.vercel

# Visual brainstorming mockups
.superpowers/

# MCP configuration files (contain credentials)
.mcp.json
.cursor/mcp.json
.claude/mcp.json
.claude/*.local.md
.claude/*.local.json

# Git worktrees
.worktrees/
.claude/worktrees/

# Claude Code — tool-specific dirs and installed skill packages
# skills-lock.json + .claude/skills/ are committed; .agents/ is the install cache
.agents/
.kiro/
.windsurf/
scripts/kill-dev.ps1

# Performance traces
perf-trace-*.json.gz

# Screenshots and temp artifacts
screenshots/
_tmp_*

# Design handoff artifacts — ignore zips and extracted contents (see MANIFEST.md for recovery)
design/handoff/**/extracted/
design/handoff/**/*.zip

# Figma token export — generated by pnpm tokens:export, not committed
figma-export/
.env*

# Domscribe artifacts
.domscribe
